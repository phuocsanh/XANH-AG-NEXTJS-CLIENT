/**
 * JsonLd Component
 * Component để thêm structured data (JSON-LD) vào page
 */
export function JsonLd({ data }: { data: object }) {
  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }}
    />
  )
}
